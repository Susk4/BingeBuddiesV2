/**
 * Firebase was removed. This module documents the old env vars — do not use.
 *
 * Data and auth now go through:
 * - `NEXT_PUBLIC_GOOGLE_CLIENT_ID` + `backend` Google token verification
 * - `src/services/DataService.ts` (API placeholders)
 */

export {};
